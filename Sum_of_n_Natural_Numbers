#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {

    int a ,b , sum=0;
    scanf("%d",&a);
    for(b=0;b<=a;b++)
    {
        sum = sum + b;
    }  
     printf("%d",sum);
    return 0;
}
