#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a,b;
    scanf("%d",&b);
    for(a=1;a<=b;a++)
    {
        if ( a%400==0 || a%4==0 && a%100!=0 )
            printf("%d ",a);
        }
        
    return 0;
}
